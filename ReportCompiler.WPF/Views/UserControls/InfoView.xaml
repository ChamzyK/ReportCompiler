<UserControl x:Class="ReportCompiler.WPF.Views.UserControls.InfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReportCompiler.WPF.Views.UserControls"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">

    <DockPanel Width="250" MaxHeight="400">
        
        <Button Content="Закрыть"
                DockPanel.Dock="Bottom"
                Margin="0"
                Command="{x:Static md:DialogHost.CloseDialogCommand}"/>
        
        <GroupBox md:ShadowAssist.ShadowDepth="Depth3"
                  BorderThickness="0">
            
            <GroupBox.Header>
                <TextBlock Text="{Binding Header}"
                           Style="{DynamicResource MaterialDesignHeadline6TextBlock}"/>
            </GroupBox.Header>
            
            <TextBlock Margin="6"
                       TextWrapping="Wrap"
                       Text="{Binding Information}" />
        </GroupBox>
    </DockPanel>
    
</UserControl>
